from operations import *

a, b, c, d = (10, 1, 0, 10)
# Adição
print(f"{a} + {b} = {adicionar(a, b)}" )
# Subtração
print(f"{b} - {d} = {subtrair(b, d)}" )
# Multiplicação
print(f"{b} * {d} = {multiplicar(b, d)}")
# Divisão
print(f"{a} / {c} = {dividir(a, c)}")